{
  "version": 2,
  "buildCommand": "yarn vercel-build",
  "outputDirectory": "packages/avail-wallet/build",
  "installCommand": "yarn install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
