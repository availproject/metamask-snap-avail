{
  "version": 2,
  "buildCommand": "yarn workspace @avail-project/avail-snap build && yarn workspace @avail-project/metamask-avail-adapter build && yarn workspace avail-wallet build",
  "outputDirectory": "build",
  "installCommand": "yarn install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
